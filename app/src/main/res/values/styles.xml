<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.SplashScreen" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your splash screen theme here -->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Add other splash screen attributes if needed -->
    </style>
    <style name="AppTheme.TransparentActionBar" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:actionBarStyle">@style/TransparentActionBarStyle</item>
    </style>

    <style name="TransparentActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/transparent</item>
    </style>

</resources>